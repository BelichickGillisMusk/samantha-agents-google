# Local Open WebUI / Ollama persistent volumes (created by docker-compose.yml).
# These hold local models, chats, and uploaded documents — never commit them.
/ollama/
/open-webui/

# Python bytecode (chat.py is module-importable for tests)
__pycache__/
*.pyc
